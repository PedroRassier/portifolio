import styled from "styled-components";

export const AboutSection_ = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #ffffff;
  h2 {
    border-bottom: 1px solid #d676de;
    font-family: var(--roboto);
    margin-bottom: 3rem;
  }
  p {
    width: 50rem;
    font-family: var(--inter);
  }
`;
